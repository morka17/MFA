apiVersion: v1
kind: Service
metadata:
  name: nodejs-mfa-api-service
spec:
  selector:
    app: nodejs-mfa-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 3000
  type: LoadBalancer
