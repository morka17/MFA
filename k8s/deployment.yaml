apiVersion: apps/v1
kind: Deployment
metadata:
  name: nodejs-mfa
spec:
  selector:
    matchLabels:
      app: nodejs-mfa
  template:
    metadata:
      labels:
        app: nodejs-mfa
    spec:
      containers:
      - name: nodejs-mfa
        image: mfa:latest
        resources:
          limits:
            memory: "128Mi"
            cpu: "500m"
        ports:
        - containerPort: 3000
        imagePullPolicy: Never
